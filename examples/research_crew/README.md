# Research Crew Example

This example folder is reserved for a parallel-execution crew scenario.

Planned coverage:
- Multiple agents running in parallel
- Assertions around parallel timing and handoffs
- Example tool-call validation
